---

- hosts: all
  become: true
  tasks:

  - name: install apache2 package
    apt:
      name: apache2
      state: absent
      purge: yes
    when:
      - ansible_facts['distribution'] == "Ubuntu"
